package component:webgpu;

/// wit for the examples in this repo
world example {
  import webgpu;
  import graphics-context;
  import mini-canvas;
  import animation-frame;
  import pointer-events;
  import key-events;
  import simple-buffer;

  /// main entrypoint
  export start: func();

  /// print to stdout
  import print: func(s: string);
}
